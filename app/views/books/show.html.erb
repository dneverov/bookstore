<p id="notice"><%= notice %></p>

<h1><%= @book.name %></h1>

<p>
  <%= @book.description %>
</p>

<%= image_tag("/books/#{@book.id}/cover/#{@book.cover_filename}", :style => "max-width: 330px;") if @book.cover_filename? %>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>
